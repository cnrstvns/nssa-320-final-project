version: '3.8'
services:
  web:
    image: 'connorstevens/nssa-320-final-project:latest'
    ports:
      - target: 3000
        published: 3000
        protocol: tcp
        mode: host
    deploy:
      replicas: 18
